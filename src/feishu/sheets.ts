/**
 * Phase 2 placeholder. Spreadsheet APIs intentionally remain isolated from the
 * document client so they can be added without changing first-phase tools.
 */
export const SHEETS_PHASE = 2 as const;
